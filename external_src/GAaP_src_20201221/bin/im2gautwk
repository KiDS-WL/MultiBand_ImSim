#!/bin/tcsh

#$1 is image
#$2 is kernel map
#$3 is output name

if $# != 3 then
   echo Usage: im2gau image kernalmap outputimname
   exit
   endif
   
set code = ~/data/shapelets/kk
set bigim = $code/bigim

ln -sf $1 inimage.fits

$bigim/imxshmapwithtweak < $2
\mv -f convolved.fits $3

ls -l $3
